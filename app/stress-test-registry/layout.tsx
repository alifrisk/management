import { ReactNode } from 'react'

export const metadata = { title: 'Реестр стресс-тестов — Алиф Банк СУР' }

export default function StressTestRegistryLayout({ children }: { children: ReactNode }) {
  return <>{children}</>
}
